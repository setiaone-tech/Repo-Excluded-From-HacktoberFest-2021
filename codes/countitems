# Define the string

string = 'Hey there how are you'

# Define the search

search = 'how'

# Store the count

count = string.count(search)

# Print the output

print("%s appear %d time" % (search, count))
